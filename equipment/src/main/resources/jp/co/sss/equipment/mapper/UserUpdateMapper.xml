<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- mapperインターフェスと修飾名を一致させる -->
<mapper namespace="jp.co.sss.equipment.mapper.UserUpdateMapper">
	<update id="userUpdate"
		parameterType="jp.co.sss.equipment.form.UserForm">
		UPDATE staff_data
		SET
		staff_no = #{staffNo},
		name = #{name},
		mail = #{mail},
		auth_no = #{auth},
		password =
		#{password}
		WHERE staff_no = #{oldStaffNo}
	</update>

</mapper>